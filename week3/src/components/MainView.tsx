import styled from "styled-components";

export default function MainView() {
  return <MainWrapper>MainView</MainWrapper>;
}

const MainWrapper = styled.main`
  background-color: rgb(37, 38, 37);
`;
